# v1.0 Stable Verification

Verification date: 2026-07-10

Evidence branches:

- `codex/v1.0-stable-verification`
- `codex/v1.0-final-operational-evidence`
- `codex/v1.0-ui-auth-flow`
- `codex/ui-ready-auth-fix`
- `codex/v1.0-live-evidence`

Release under verification: `1.0.0`

Status: **authenticated endpoint, UI readiness, approval-flow smoke, and audit
persistence passed; notification, n8n outage, and backup/restore evidence
pending**

The local source tree passes the full automated quality gate, and the Kelvin VM
serves the v1.0 application build. Production/LAN auth is enabled, the runtime
status endpoint reports `ready`, protected operator APIs accept the scoped
operator token, and a live chat turn completed against the configured local
Ollama model. The UI auth flow and authenticated readiness checks were deployed
after the original blocker was found, and the operator confirmed the UI works
with the session-only API token control.

Final completion is still waiting on recorded live evidence for email
notifications, n8n outage behavior, and backup/restore execution.

---

## 1. Automated Quality Gate

Command set:

```powershell
ruff check backend tests scripts
ruff format --check backend tests scripts
mypy backend/src tests
pytest --cov=kelvin_assistant --cov-report=term-missing --basetemp=.pytest-basetemp
git diff --check
```

Result:

- Ruff lint: passed.
- Ruff format check: passed.
- mypy: passed for 144 source files.
- pytest: 445 passed.
- Coverage: 84%.
- `git diff --check`: passed.

Warnings observed:

- `StarletteDeprecationWarning` for `HTTP_422_UNPROCESSABLE_ENTITY` in
  `tests/unit/api/test_settings_endpoint.py::test_update_settings_validation_errors`.
- `DeprecationWarning` for `datetime.datetime.utcnow()` in
  `tests/unit/domain/test_firewall.py::test_sanitize_external_content`.

These warnings match the known Step 9 cleanup candidates in
`docs/ai/v10-readiness-audit.md`.

---

## 2. Live VM Endpoint Checks

Target checked:

```text
http://192.168.10.13:8000
```

### Initial blocked check

Observed before VM upgrade:

| Endpoint | Result |
|---|---|
| `/health` | `{"status":"ok"}` |
| `/status` | HTTP 404 `{"detail":"Not Found"}` |
| `/ready` | `{"status":"ready","provider":"ollama","model":"gemma4:e4b"}` |
| `/ready/database` | `{"status":"ready","provider":"postgresql"}` |
| `/version` | `{"version":"0.6.0"}` |

This failed the v1.0 operational verification precondition.

### Post-upgrade check

Observed after the VM package update from `kelvin-assistant==0.6.0` to
`kelvin-assistant==1.0.0`:

| Endpoint | Result |
|---|---|
| `/health` | `{"status":"ok"}` |
| `/status` | `{"status":"unavailable","components":[{"name":"api","status":"ready","required":true,"detail":"FastAPI process is running."},{"name":"auth","status":"disabled","required":true,"detail":"API authentication is disabled; use only for local development."},{"name":"llm","status":"ready","required":true,"detail":"ollama:gemma4:e4b"},{"name":"database","status":"ready","required":true,"detail":"PostgreSQL connection is ready."},{"name":"n8n","status":"unconfigured","required":false,"detail":"n8n is optional and not configured."}]}` |
| `/ready` | `{"status":"ready","provider":"ollama","model":"gemma4:e4b"}` |
| `/ready/database` | `{"status":"ready","provider":"postgresql"}` |
| `/version` | `{"version":"1.0.0"}` |

Interpretation:

- The VM process is alive.
- The VM now serves v1.0 code.
- The v1.0 `/status` endpoint is available.
- Ollama readiness passed.
- PostgreSQL readiness passed.
- n8n is optional and unconfigured.
- Overall `/status` is `unavailable` because auth is disabled while auth is
  required for the configured runtime profile.

This keeps final v1.0 operational verification blocked until production auth is
enabled and the smoke checks are rerun.

### Post-auth check

Observed after configuring `KELVIN_API_AUTH_MODE=required` and creating
`/etc/kelvin-assistant/api-tokens.json`:

| Endpoint | Result |
|---|---|
| `/health` | `{"status":"ok"}` |
| `/status` | `{"status":"ready","components":[{"name":"api","status":"ready","required":true,"detail":"FastAPI process is running."},{"name":"auth","status":"ready","required":true,"detail":"API authentication is required."},{"name":"llm","status":"ready","required":true,"detail":"ollama:gemma4:e4b"},{"name":"database","status":"ready","required":true,"detail":"PostgreSQL connection is ready."},{"name":"n8n","status":"unconfigured","required":false,"detail":"n8n is optional and not configured."}]}` |
| `/version` | `{"version":"1.0.0"}` |

Interpretation:

- The VM process is alive.
- The VM serves v1.0 code.
- Production/LAN auth is enabled and required.
- Ollama readiness is ready.
- PostgreSQL readiness is ready.
- n8n remains optional and unconfigured.
- Aggregate runtime status is `ready`.

---

### Post-UI-auth deployment check

Observed on 2026-07-10 after deploying the UI auth flow, the authenticated
readiness fix, and the VM update helper:

| Endpoint | Result |
|---|---|
| `/health` | HTTP 200 `{"status":"ok"}` |
| `/status` | HTTP 200 `{"status":"ready","components":[{"name":"api","status":"ready","required":true,"detail":"FastAPI process is running."},{"name":"auth","status":"ready","required":true,"detail":"API authentication is required."},{"name":"llm","status":"ready","required":true,"detail":"ollama:gemma4:e4b"},{"name":"database","status":"ready","required":true,"detail":"PostgreSQL connection is ready."},{"name":"n8n","status":"unconfigured","required":false,"detail":"n8n is optional and not configured."}]}` |
| `/ready` | HTTP 200 `{"status":"ready","provider":"ollama","model":"gemma4:e4b"}` |
| `/ready/database` | HTTP 200 `{"status":"ready","provider":"postgresql"}` |
| `/version` | HTTP 200 `{"version":"1.0.0"}` |

Protected operator API checks with the `ps-client` token:

| Endpoint | Result |
|---|---|
| `/api/v1/agent/runs` | HTTP 200, existing run list returned. |
| `/api/v1/settings` | HTTP 200, runtime/settings payload returned with `email_notifications_enabled:false` and `n8n_url:null`. |
| `/api/v1/security/audit` | HTTP 200, empty audit list returned. |
| `/api/v1/n8n/health` | HTTP 200 `{"status":"unconfigured","configured":false,...}` |
| `POST /api/v1/chat` | HTTP 200, `{"message":"OK","model":"gemma4:e4b",...}` for `Say OK`. |

Interpretation:

- Production/LAN auth is enforced and accepts the scoped operator token.
- The VM serves the current v1.0 code after the update script deployment.
- The configured Ollama model is reachable for both readiness and an actual
  chat turn.
- PostgreSQL readiness passes.
- n8n is optional and unconfigured; this is non-blocking.
- Email notifications were disabled at this checkpoint; the live follow-up
  below accepts test email and approval-required notification delivery.

---

### Approval, guard, audit, and notification check

Observed on 2026-07-10 with the scoped `ps-client` token:

| Check | Result |
|---|---|
| `POST /api/v1/agent/runs` | HTTP 201 created evidence run `b9f35dd1-e7dd-4052-be45-9df84a8a6256`. |
| `POST /api/v1/agent/runs/{id}/plan` | HTTP 200, run moved to `planning`. |
| `POST /api/v1/agent/runs/{id}/tools` with `file.patch` | HTTP 200, policy `require_approval`, approval `pending`, run `awaiting_approval`. |
| `GET /api/v1/agent/runs/{id}/tools/active` | HTTP 200, active proposal returned with approval `pending`. |
| `POST /api/v1/agent/runs/{id}/approval` with `rejected` | HTTP 200, proposal approval `rejected`, run `cancelled`. |
| `GET /api/v1/agent/runs/{id}` | HTTP 200, one step returned with `approval_status:"rejected"`. |
| unsafe `POST /api/v1/agent/runs` asking to read `.env` and `api_key` | HTTP 400, blocked by input guard with credential warnings. |
| `GET /api/v1/security/audit?event_type=input_guard&decision=block` | HTTP 200, but returned an empty list after the blocked request. |
| `POST /api/v1/settings/test-email` | HTTP 400, `Email notifications are currently disabled in settings.` |
| `POST /api/v1/settings/send-summary` | HTTP 400, `Email notifications are currently disabled in settings.` |

Interpretation:

- The approval queue and approval resolution path are working with production
  auth enabled.
- The input guard blocks credential-harvesting requests in the live VM.
- At this point in the verification attempt, audit persistence was not accepted
  yet because the expected blocked input entry was not returned. The follow-up
  check below closes this gap after the VM schema update fixes.
- Notification delivery is not accepted yet because email notifications are
  disabled in the live settings.

---

### Audit persistence follow-up

Observed on 2026-07-10 after deploying the VM update helper fixes and running a
full VM update without `--skip-db-schema`:

| Check | Result |
|---|---|
| `/health` | HTTP 200 `{"status":"ok"}` |
| `/ready` | HTTP 200 `{"status":"ready","provider":"ollama","model":"gemma4:e4b"}` |
| unsafe `POST /api/v1/chat` asking to read `.env` and extract `api_key` values | HTTP 400, blocked by input guard with credential warnings. |
| `GET /api/v1/security/audit?event_type=input_guard&decision=block&limit=5` | HTTP 200, returned one blocked input-guard audit entry. |

Returned audit evidence:

- `id`: `b87425bc-0c01-46dd-b8ae-e755013f889a`
- `correlation_id`: `8d9094fe-b48e-4125-9b26-d2a9ed3fc659`
- `event_type`: `input_guard`
- `decision`: `block`
- `warnings`:
  - `Attempt to read environment configuration file (.env) detected`
  - `Attempt to extract database or API credentials detected`

Interpretation:

- Live input-guard blocking is persisted to PostgreSQL-backed security audit
  logs.
- The audit API can query the blocked entry through the scoped operator token.
- Audit persistence is accepted for v1.0 evidence.

---

### Email and notification follow-up

Observed on 2026-07-11 after configuring SMTP email through `/ui/settings`,
updating the VM, and restarting `kelvin-api`:

| Check | Result |
|---|---|
| Settings save | Operator confirmed email settings saved through the UI. |
| Test email | Operator confirmed the test email was received. |
| Restart persistence | Operator confirmed email settings remained configured after restart/update. |
| `POST /api/v1/agent/runs` | HTTP 201 created evidence run `3819ed82-b7e6-4aea-a3b1-bedcaa5d72ca`. |
| `POST /api/v1/agent/runs/{id}/plan` | HTTP 200, run moved to `planning`. |
| `POST /api/v1/agent/runs/{id}/tools` with `file.patch` | HTTP 200, policy `require_approval`, approval `pending`, run `awaiting_approval`; tool call `7538f2a4-b44c-419c-92c6-7a74c498e5cf`. |
| Approval email | Operator confirmed the approval-required email was received. |
| `POST /api/v1/agent/runs/{id}/approval` with `rejected` | HTTP 200, proposal approval `rejected`, run `cancelled`, no file change executed. |
| Daily summary email | Operator confirmed the daily summary email was received and that the content was useful and safe. |
| Failed run notification | Operator confirmed a failed-run notification email was received after an intentional read-tool failure smoke test. |
| Completed run notification | HTTP 200 completion response for run `642f6196-993b-4d8e-9aeb-eb8e72007d18`, action `complete`, run status `completed`, safe summary returned; operator confirmed the completed-run notification email was received. |

Interpretation:

- Settings persistence is accepted after restart.
- SMTP test delivery is accepted.
- Approval-required notification delivery is accepted.
- Daily summary delivery is accepted.
- Failed-run and completed-run notification delivery is accepted.
- The notification emails could use better formatting, but this is a post-v1.0
  usability improvement rather than a release blocker.

---

## 3. UI Verification

### Historical operational UI auth blocker

Observed on 2026-07-09 against:

```text
http://192.168.10.13:8000
```

The following UI pages return static HTML successfully and do not show server
errors:

| Page | Observed result |
|---|---|
| `/ui` | Page loads with chat shell. |
| `/ui/runs` | Page loads, but run data shows `Hiba a futások betöltésekor: Sikertelen betöltés`. |
| `/ui/approvals` | Page loads, but approval data shows `Hiba: Sikertelen betöltés`. |
| `/ui/audit` | Page loads, but audit data shows `Hiba a betöltéskor: Sikertelen API lekérdezés`. |
| `/ui/settings` | Page loads, but settings data shows `Hiba a beállítások betöltésekor: Sikertelen betöltés`. |
| `/ui/n8n` | Page loads, but n8n status shows `Hiba részletei: Sikertelen API lekérdezés`. |

The matching protected API calls without a bearer token return `401`:

| Method | Endpoint | Result |
|---|---|---|
| `GET` | `/api/v1/agent/runs` | `401` |
| `GET` | `/api/v1/security/audit` | `401` |
| `GET` | `/api/v1/settings` | `401` |
| `GET` | `/api/v1/n8n/health` | `401` |
| `POST` | `/api/v1/settings/test-email` | `401` |
| `POST` | `/api/v1/settings/send-summary` | `401` |

The frontend scripts call these protected routes with plain `fetch(...)`
requests and do not attach an `Authorization: Bearer ...` header. This is
consistent with the historical ADR note that the frontend did not yet receive
an API token. With `KELVIN_API_AUTH_MODE=required`, this prevents final
operator verification of runs, approvals, audit, settings, n8n health, test
email, and daily summary from the UI.

### Authenticated UI follow-up

Remediation deployed:

- `backend/src/kelvin_assistant/web/auth.js` adds the header API token control
  and stores the raw bearer token only in browser session storage.
- Runs, Approvals, Audit, Settings, n8n, and Chat UI scripts now use
  authenticated API requests for protected endpoints.
- Runtime status checks now call protected `/ready` through `authFetch`, which
  fixes the false `A modell nem érhető el` UI state under production auth.
- Static script cache keys were bumped so the VM browser loads the fixed
  assets.

Live result:

- Operator confirmed the UI works after setting the raw token with the header
  API token button.
- Operator confirmed the false model-unavailable status is gone after updating
  the VM.
- Operator confirmed in Chrome that `/ui/audit` loads the audit table after
  setting the raw `ps-client` token with the header API token button.

---

### Token rotation follow-up

Observed on 2026-07-11 after the raw `ps-client` token was accidentally pasted
into chat during UI verification:

| Check | Result |
|---|---|
| Token rotation guide | `docs/token-management.md` and `docs/operational-runbooks.md` now document raw-token vs SHA-256 usage, VM rotation, and UI token refresh steps. |
| Operator rotation | Operator confirmed the guide worked and the exposed raw token was refreshed. |

Interpretation:

- The exposed raw bearer token is no longer the active operator token.
- The VM token file continues to store only SHA-256 digests.
- The UI token refresh path is documented for future rotations.

---

## 4. Checks Not Accepted Yet

The following checks are not accepted for v1.0 from this attempt because they
still need recorded operational evidence:

- n8n outage behavior
- backup and restore verification

---

## 5. Required Remediation

Before marking v1.0 complete:

1. Record n8n outage behavior while keeping local Kelvin features usable.
2. Record backup and restore verification evidence.
3. Rerun the full endpoint, UI, notification, n8n outage, backup, and restore
   evidence checklist from `docs/operational-runbooks.md`.
4. Only then mark Step 9 and v1.0 complete in `docs/ai/v10-guide.md` and
   `docs/roadmap.md`.
